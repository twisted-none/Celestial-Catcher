# Celestial-Catcher 🌠

**Celestial-Catcher** — это распределенная система для сбора, обработки и визуализации космических данных. Проект демонстрирует интеграцию разнородных сервисов, включая бэкенд на Rust, фронтенд на Laravel (PHP), легаси-модуль на Pascal и инфраструктурные компоненты (PostgreSQL, Redis) в единой Docker-среде.

## Архитектура проекта

Система состоит из нескольких ключевых сервисов, работающих в Docker:

*   **`nginx` (Веб-сервер/Прокси):** Единая точка входа. Принимает все запросы на порт 80, направляя их либо на фронтенд (Laravel), либо на бэкенд (Rust API).
*   **`php-web` (Фронтенд):** Веб-приложение на **Laravel 12** с использованием Bootstrap. Представляет собой дашборд для визуализации данных (карты, графики, таблицы). Напрямую читает данные из PostgreSQL.
*   **`rust-iss` (Бэкенд-сборщик):** Высокопроизводительный сервис на **Rust**, написанный с использованием фреймворка Axum. Его задачи:
    *   В фоновом режиме опрашивать внешние космические API (эмулируется сбор данных о положении МКС).
    *   Записывать сырые данные (JSON) в базу данных PostgreSQL.
    *   Предоставлять собственный REST API, доступный через проксирование в Nginx.
*   **`pascal-legacy` (Легаси-модуль):** Контейнеризированная утилита на **Pascal**, которая периодически генерирует данные в форматах CSV и XLSX, эмулируя работу вспомогательной системы.
*   **`postgres` (База данных):** Основное хранилище данных на **PostgreSQL**. Хранит сырые логи от Rust-сервиса, кэши и другую информацию.
*   **`redis` (Кэш/Брокер):** In-memory хранилище, используемое для кэширования, управления очередями и реализации Rate Limiting.

### Схема взаимодействия
`Пользователь` -> `Nginx` -> (`/`) `php-web` <-> `postgres`
                        -> (`/api`) `rust-iss` -> `postgres`

## Технологический стек

- **Бэкенд:** Rust (Axum, SQLx)
- **Фронтенд:** PHP 8.4, Laravel 12
- **База данных:** PostgreSQL 15
- **Кэш:** Redis 7
- **Легаси:** Free Pascal
- **Оркестрация:** Docker, Docker Compose
- **Веб-сервер:** Nginx

## Запуск проекта

**Требования:**
*   Docker
*   Docker Compose

**Инструкция:**

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [ссылка на твой репозиторий]
    cd celestial-catcher
    ```

2.  **Соберите и запустите контейнеры:**
    Выполните команду в корне проекта. Первая сборка может занять несколько минут, особенно для Rust-сервиса.
    ```bash
    docker compose up --build -d
    ```
    Флаг `-d` запустит контейнеры в фоновом режиме.

3.  **Проверка работоспособности:**
    *   **Дашборд:** Откройте в браузере `http://localhost`. Вы должны увидеть главный дашборд. Данные в таблице появятся через 10-20 секунд, после того как Rust-сервис сделает первую запись в БД.
    *   **Логи:** Чтобы посмотреть "живые" логи работы сервисов, используйте команду:
        ```bash
        # Посмотреть логи Rust-сборщика
        docker compose logs -f rust-iss
        
        # Посмотреть логи всех сервисов
        docker compose logs -f
        ```

4.  **Остановка проекта:**
    ```bash
    docker compose down
    ```

5.  **Остановка с удалением данных (томов):**
    Если вы хотите полностью очистить базу данных и начать с нуля:
    ```bash
    docker compose down -v
    ```